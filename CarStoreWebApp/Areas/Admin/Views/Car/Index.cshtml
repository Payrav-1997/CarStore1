
@model List<CarStoreWebApp.Models.Car>
@{
    ViewData["Title"] = "CarStore";
}

<a class="btn btn-dark" asp-action="Add" asp-controller="Car">Добавить</a>
<table class="table table-active table-bordered table-hover">
    <thead>
        <tr>
            <td><d>ID</d></td>
            <td><d>Название</d></td>
            <td><d>Цена</d></td>
            <td><d>Фотография</d></td>
            <td><d>Описание</d></td>
            

        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.Id</td>
                <td>@x.Name</td>
                <td>@x.Price</td>
                <td>@x.Img</td>
                <td>@x.Desciption</td>             
                <td><a class="btn btn-danger" asp-action="Delete" asp-controller="Car" asp-route-id="@x.Id" id="Delete">Удалить</a> <a class="btn btn-primary" asp-action="Edit" asp-controller="Car" asp-route-id="@x.Id">Изменить</a></td>
            </tr>
        }
    </tbody>
</table>



