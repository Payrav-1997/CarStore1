
@model List<CarStoreWebApp.Models.Model>
@{
    ViewData["Title"] = "CarStore";
}

<a class="btn btn-dark" asp-action="Add" asp-controller="Model">Добавить</a>
<table class="table table-active table-bordered table-hover">
    <thead>
        <tr>
            <td><d>ID</d></td>
            <td><d>Название</d></td>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.Id</td>
                <td>@x.Name</td>
                <td><a class="btn btn-danger" asp-action="Delete" asp-controller="Model" asp-route-id="@x.Id" id="Delete">Удалить</a> <a class="btn btn-primary" asp-action="Edit" asp-controller="Model" asp-route-id="@x.Id">Изменить</a></td>
            </tr>
        }
    </tbody>
</table>

