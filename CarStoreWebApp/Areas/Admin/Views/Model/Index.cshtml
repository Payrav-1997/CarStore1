
@model List<CarStoreWebApp.Models.Model>
@{
    ViewData["Title"] = "CarStore";
}

<a asp-action="Add" asp-controller="Model">Добавить</a>
<table class="table table-active table-bordered table-hover">
    <thead>
        <tr>
            <td>ID</td>
            <td>Название</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.Id</td>
                <td>@x.Name</td>
                <td><a asp-action="Delete" asp-controller="Model" asp-route-id="@x.Id" id="Delete">Удалить</a> <a asp-action="Edit" asp-controller="Model" asp-route-id="@x.Id">Изменить</a></td>
            </tr>
        }
    </tbody>
</table>

