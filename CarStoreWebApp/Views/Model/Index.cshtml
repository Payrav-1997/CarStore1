
@model List<CarStoreWebApp.Models.Model>
@{
    ViewData["Title"] = "CarStoer";
}

<a asp-action="Index" asp-controller="Model">Add</a>
<table class="table table-active table-bordered table-hover">
    <thead>
        <tr>
            <td>ID</td>
            <td>Название</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.Id</td>
                <td>@x.Name</td>
                <td><a asp-action="Delete" asp-controller="Model" asp-route-id="@x.Id" id="Delete">Delete</a> <a asp-action="Edit" asp-controller="Model" asp-route-id="@x.Id">Edit</a></td>
            </tr>
        }
    </tbody>
</table>



